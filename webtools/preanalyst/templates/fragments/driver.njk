{# Solo gli interni di [data-sso-driver]: li rimpiazza il browser dopo il login. #}
{% import "partials/driver_box.njk" as box %}
{% import "partials/driver_work.njk" as lavoro %}
{% if aside.driver_box.show %}{{ box.render(aside.driver_box) }}{% endif %}{% if aside.driver_work.show %}{{ lavoro.render(aside.driver_work) }}{% endif %}
