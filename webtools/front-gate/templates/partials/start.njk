{#- The "Inizia" button: it leads to the pre-analysis form, in the same tab.
    The address comes from the front-gate's configuration, read at startup:
    without it the server does not start. -#}
<a class="btn btn-primary gateway-start" href="{{ preanalystUrl }}" data-gateway="pre-analysis">{{ t("front_gate.start") }}</a>
