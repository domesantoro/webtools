{#- Il tasto "Inizia": porta al form della pre-analisi, nella stessa scheda.
    L'indirizzo arriva dalla configurazione del front-gate, letta all'avvio:
    senza, il server non parte. -#}
<a class="btn btn-primary gateway-start" href="{{ preanalystUrl }}" data-gateway="pre-analysis">{{ t("front_gate.start") }}</a>
